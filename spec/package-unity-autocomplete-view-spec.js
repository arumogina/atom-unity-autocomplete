'use babel';

import PackageUnityAutocompleteView from '../lib/package-unity-autocomplete-view';

describe('PackageUnityAutocompleteView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
